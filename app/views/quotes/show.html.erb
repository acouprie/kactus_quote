<% content_for :title, @quote.name %>

<div class="page">
  <% if @quote.draft? %>
    <%= render "draft_screen", quote: @quote, totals: @totals %>
  <% else %>
    <%= render "validated_screen", quote: @quote, totals: @totals %>
  <% end %>
</div>
